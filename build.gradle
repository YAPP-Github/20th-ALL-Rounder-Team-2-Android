buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath(libs.android.gradle)
        classpath(libs.kotlin)
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
